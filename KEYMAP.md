# 실측 키 매핑 (iOS)

카카오 제작가이드(`26.7.0_iOS`)가 화면 요소와 CSS 키를 연결해 주지만, **여러 군데가
현재 카카오톡과 어긋납니다.** 아래는 진단용 테마 — 키마다 서로 다른 형광색을 넣은
`.ktheme` — 를 실제 기기에 적용하고 스크린샷 픽셀을 재서 확인한 결과입니다.

측정일 2026-09-15, 카카오톡 iOS, 채팅탭 / 통화 필터 화면 기준.

## 가이드가 틀린 곳

| 화면 요소 | 실제 키 | 가이드 표기 |
|---|---|---|
| 통화 발신 버튼의 원 | `MainViewStyle-Primary` `-ios-text-color` | **없음** |
| 헤더 아이콘 (검색·채팅추가·설정) | `MainViewStyle-Primary` `-ios-text-color` | `HeaderStyle-Main` 이라고 적힘 |
| 칩 텍스트 (전체·안읽음·채널…) | `HeaderStyle-Main` `-ios-text-color` | `MainViewStyle-Primary` 라고 적힘 |
| 리스트 날짜 | `-ios-paragraph-text-color` | **없음** |
| 선택된 칩 배경 | `#191919` **카카오톡 고정** | `-ios-text-color` 라고 적힘 |
| 미선택 칩 배경 | `#F5F5F5` **카카오톡 고정** | **없음** |

## 가이드가 맞는 곳

| 화면 요소 | 키 |
|---|---|
| 리스트 이름 (프로필명·채팅방 이름) | `MainViewStyle-Primary` `-ios-text-color` |
| 헤더 타이틀 ("채팅") | `HeaderStyle-Main` `-ios-text-color` |
| 라스트메시지 | `-ios-paragraph-text-color` |
| 상태메시지 | `-ios-description-text-color` |
| 더보기탭 그리드 영역 배경 | `-ios-text-color` 알파 6% |

## `-ios-text-color` 한 키에 묶인 것들

이 키 하나가 아래를 전부 결정합니다. 하나만 따로 조정할 수 없습니다.

- 리스트 이름 (100%)
- **통화 발신 버튼의 원 (100%, 꽉 찬 면)**
- 헤더 아이콘 (100%)
- 더보기탭 그리드 영역 배경, 게임플레이 배경 (알파 6%)
- 날씨 GPS 아이콘 (알파 70%), GPS 버튼 보더 (알파 20%)

타일과 배경의 차이는 `0.06 x (배경 - T)` 라 **배경색과 무관하고 `-ios-text-color` 만이
정합니다.** 그래서 배경을 순백으로 올려도 타일의 무게는 달라지지 않습니다.

## 기본 테마는 재현할 수 없다

같은 화면을 기본 테마와 사용자 테마에서 각각 재면 이렇습니다.

| | 기본 테마 | 사용자 테마 (`-ios-text-color: #666666`) |
|---|---|---|
| 통화 버튼 원 | `#F5F5F5` | `#666666` |
| 리스트 이름 | `#191919` | `#666666` |

**기본 테마는 원을 거의 흰색으로, 이름을 거의 검정으로 따로 줍니다.** 사용자 테마에서는
둘이 한 키로 합쳐져 반드시 같은 값이 됩니다. 원을 밝히려면 이름이 같이 사라지므로,
기본 테마의 인상은 사용자 테마로 만들 수 없습니다.

## 다시 측정하는 법

키를 하나씩 추측으로 바꿔 보는 것보다, 미선언 키를 전부 서로 다른 형광색으로 칠한
`.ktheme` 를 한 번 적용하고 문제 화면을 찍는 쪽이 빠릅니다. `theme-id` 를 본 테마와
다르게 두면 둘 다 설치되어 설정에서 오갈 수 있습니다.

초록은 통화 수화기 아이콘과 겹치므로 피합니다. 스크린샷이 Display P3 라 `#FF0000` 이
`#EA3323` 으로 읽히는 등 값이 정확히 일치하지는 않으므로, 절대값이 아니라 **어느
형광색 계열인지**로 판단합니다.
